header:
  version: 20

repos:
  meta-foo:
    path: meta-foo

local_conf_header:
  meta-foo: |
    IMAGE_INSTALL:append = " nano fake-hwclock hello raylib hello-raylib can-bus-raylib"
    INIT_MANAGER = "systemd"
    INHERIT += "rm_work"
